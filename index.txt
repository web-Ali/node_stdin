
const readline = require('readline');

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

let arr = [];
let percent=0;
rl.on('line', line => {
  arr.push(line);
  if(arr.length === 2) {
  
   rl.close();
}
  
})
